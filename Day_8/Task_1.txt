LeetCode 53

class Solution {
    public int maxSubArray(int[] nums) {
        int len=nums.length,sum=0,max=nums[0];
        for(int i=0;i<len;i++){
            sum+=nums[i];
            max=Math.max(sum,max);
            if(sum<0) sum=0;        
        }
        return max;  
    }
}
